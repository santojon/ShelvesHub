# Release Notes

This project uses `CHANGELOG.md` to track notable changes. Releases are generated automatically by CI when a tag matching `v*.*.*` is pushed.

For detailed release contents, see `CHANGELOG.md` and the corresponding GitHub release created by the CI pipeline.
