{
  "_comment": "DeckProbe config for ShelvesHub. Loaded with .env (DECK_CDP_HOST/PORT); drive via the deck:probe / deck:diag / deck:screenshot / deck:perf scripts. DECKPROBE_* env vars override any value.",
  "screenshots_dir": "screenshots/deck",
  "selectors": {}
}
